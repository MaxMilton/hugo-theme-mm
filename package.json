{
  "name": "mm",
  "version": "0.0.0",
  "description": "🎨 Hugo theme used on https://MaxMilton.com",
  "repository": "git@github.com:MaxMilton/hugo-theme-mm.git",
  "author": "Max Milton <max@wearegenki.com>",
  "license": "MIT",
  "scripts": {
    "lint": "yarn lint:css && yarn lint:js",
    "lint:css": "stylelint **/*.{html,css} --cache",
    "lint:js": "eslint --ignore-path .gitignore --ext js,html .",
    "test": "yarn lint && jest --coverage",
    "test-ci": "jest --runInBand --ci --verbose --coverage"
  },
  "dependencies": {
    "@minna-ui/css": "^0.9.0"
  },
  "devDependencies": {
    "@minna-ui/eslint-config": "^0.9.0",
    "@minna-ui/jest-config": "^0.9.1",
    "@minna-ui/postcss-config": "^0.9.0",
    "@minna-ui/stylelint-config": "^0.9.0",
    "codecov": "^3.0.4",
    "eslint": "^5.2.0",
    "jest": "^23.4.1",
    "postcss": "^7.0.1",
    "postcss-cli": "^6.0.0",
    "stylelint": "^9.4.0"
  },
  "private": true
}
